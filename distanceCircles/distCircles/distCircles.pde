
int nTiles = 20;
float sizeTile;

void setup () {
  size(600, 600);

  sizeCell = width / float(nTiles);
}

void draw () {
  background(255);

  for (int i = 0; i < nTiles; i++) {
    for (int j = 0; j < nTiles; j++) {
    }
  }
}
